#include <stdio.h>
#include "main.h"

/**
 * add -  adds two numbers and returns results to main
 * @x: parameter passed from main
 * @y: second parameter passed from main
 * Return: returns sum of x and y;
 */

int add(int x, int y)
{
	int sum = 0;

	sum = x + y;
	return (sum);
}
