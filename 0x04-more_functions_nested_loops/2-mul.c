#include "main.h"

/**
 * mul - finds the product of 2 intergers
 * @a: 1st parameter passed.
 * @b: 2nd parameter passed.
 * Return: return product
 */

int mul(int a, int b)
{
	int product = 0;

	product = a * b;
	return (product);
}
